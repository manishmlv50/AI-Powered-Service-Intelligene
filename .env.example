# =============================================================================
# Azure OpenAI Configuration (Required)
# =============================================================================
# Azure OpenAI endpoint for your resource
AZURE_OPENAI_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/

# Azure OpenAI API key
AZURE_OPENAI_API_KEY=<your-api-key>

# API version (should be 2025-03-01-preview for Responses API)
AZURE_OPENAI_API_VERSION=2025-03-01-preview

# Deployment name for Azure OpenAI Responses API
AZURE_OPENAI_RESPONSES_DEPLOYMENT_NAME=gpt-4.1-mini
# Alternative: AZURE_OPENAI_DEPLOYMENT_NAME (used as fallback)

# =============================================================================
# Azure SQL Database Configuration (Optional - JSON fallback available)
# =============================================================================
# Azure SQL connection string
# Format: Driver={ODBC Driver 18 for SQL Server};Server=tcp:<server>.database.windows.net,1433;Database=<db>;Uid=<user>;Pwd=<password>;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;
AZURE_SQL_CONNECTION_STRING=

# Use JSON fallback instead of SQL database (set to "true" to enable)
# When enabled, the application will use synthetic JSON data files instead of Azure SQL
USE_JSON_FALLBACK=false

# =============================================================================
# Speech-to-Text Configuration (Optional - only needed for Speech-to-Text UI)
# =============================================================================
# Azure OpenAI endpoint for transcription (can be same as AZURE_OPENAI_ENDPOINT)
AZURE_OPENAI_TRANSCRIBE_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/

# Azure OpenAI API key for transcription (can be same as AZURE_OPENAI_API_KEY)
AZURE_OPENAI_TRANSCRIBE_API_KEY=<your-api-key>

# Deployment name for transcription model
AZURE_OPENAI_TRANSCRIBE_DEPLOYMENT_NAME=gpt-4o-mini-transcribe

# =============================================================================
# Azure Blob Storage Configuration (Optional - for OBD file uploads)
# =============================================================================
# Azure Storage connection string for OBD file uploads
# Format: DefaultEndpointsProtocol=https;AccountName=<account>;AccountKey=<key>;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONNECTION_STRING=

# Blob container name for storing OBD reports
BLOB_CONTAINER_NAME=obd-reports